name: Build APK with generated gradlew

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: ⬇️ Клонируем текущий проект
        uses: actions/checkout@v4

      - name: 📥 Клонируем внешний репозиторий dym
        run: git clone --depth=1 https://github.com/Luwerdwighime/dym.git external/dym

      - name: ⚙️ Устанавливаем JDK и Android SDK
        uses: android-actions/setup-android@v4
        with:
          api-level: 34

      - name: 🔨 Устанавливаем make и cmake
        run: sudo apt-get update && sudo apt-get install -y cmake make

      - name: 🧱 Сборка dym через CMake
        working-directory: external/dym
        run: |
          mkdir build && cd build
          cmake ..
          make -j$(nproc)

      - name: 📦 Кладём dym и config.ini в assets/
        run: |
          mkdir -p app/src/main/assets
          cp external/dym/build/dym app/src/main/assets/
          cp external/dym/config.sebos.ini app/src/main/assets/

      - name: ⚙️ Генерим Gradle Wrapper
        run: gradle wrapper --gradle-version 8.2

      - name: 🧙 Делаем gradlew исполняемым
        run: chmod +x gradlew

      - name: 🧪 Собираем .apk
        run: ./gradlew assembleDebug

      - name: 🚀 Загружаем .apk как артефакт
        uses: actions/upload-artifact@v4
        with:
          name: dymMob-debug.apk
          path: app/build/outputs/apk/debug/app-debug.apk

