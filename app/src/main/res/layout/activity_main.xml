<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
  android:id="@+id/scroll_root"
  android:layout_width="match_parent"
  android:layout_height="match_parent"
  android:padding="16dp">

  <LinearLayout
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:gravity="center_horizontal"
    tools:context=".MainActivity">

    <LinearLayout
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:orientation="horizontal"
      android:gravity="center_vertical"
      android:layout_marginBottom="16dp">

      <EditText
        android:id="@+id/input_url"
        android:layout_weight="1"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:hint="Вставьте URL"
        android:inputType="textUri"
        android:singleLine="true"
        android:imeOptions="actionDone" />

      <ImageButton
        android:id="@+id/paste_button"
        android:src="@android:drawable/paste"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_marginStart="8dp"
        android:contentDescription="Вставить из буфера обмена"
        android:background="?attr/selectableItemBackgroundBorderless" />
    </LinearLayout>

    <Button
      android:id="@+id/option1"
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:text="Скачать видео в видеопапку"
      android:layout_marginBottom="12dp"/>

    <Button
      android:id="@+id/option2"
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:text="Скачать аудио в аудиопапку"
      android:layout_marginBottom="12dp"/>

    <Button
      android:id="@+id/option3"
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:text="Скачать видеоплейлист в видеопапку"
      android:layout_marginBottom="12dp"/>

    <Button
      android:id="@+id/option4"
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:text="Скачать аудиоплейлист в аудиопапку"
      android:layout_marginBottom="12dp"/>

    <Button
      android:id="@+id/option5"
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:text="Докачать Дмитрия Рея" />
  </LinearLayout>
</ScrollView>

